from dragonfly import *
from caster.lib import settings

cmd.map = {
		"NOT AS CCR MODULE":				Text("c s s will be added to c c r as a Node Rule, not a dot text"),
	   
     	   
          }

cmd.extras   = [
           ]
cmd.defaults = {
           }
    
